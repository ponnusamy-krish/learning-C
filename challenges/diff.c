#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t); // Use scanf instead of cin in C

    while (t--)
    {
        int A, C;
        scanf("%d %d", &A, &C); // Use scanf instead of cin in C
        
        printf("%d %d\n",A,C);

    }

    return 0;
}